[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-plane-client"
version = "0.1.0"
description = "Python client SDK for the agent-plane server API"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27", "pydantic>=2.0,<3"]
