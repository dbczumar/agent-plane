# client

How users and programs interact with the server. Provides a CLI and a typed client library for managing agents — creating, inspecting, and deleting them — without crafting raw HTTP requests.
