# Agent-plane — installed from the private GitHub repo.
# For public repos, use: agent-plane (once published to PyPI).
# For private repos, use a deploy token or upload a wheel.
git+https://github.com/dbczumar/agent-plane.git@main

# Databricks SDK for UC Volumes artifact store and Lakebase
# OAuth token generation
databricks-sdk>=0.40.0

# PostgreSQL driver for Lakebase
psycopg[binary]>=3.1
