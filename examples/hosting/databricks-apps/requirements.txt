# Agent-plane — installed from the bundled wheel.
# Replace with 'agent-plane' once published to PyPI.
./agent_plane-0.1.0-py3-none-any.whl

# Databricks SDK for UC Volumes artifact store and Lakebase
# OAuth token generation
databricks-sdk>=0.40.0

# PostgreSQL driver for Lakebase
psycopg[binary]>=3.1
