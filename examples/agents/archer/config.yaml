spec_version: 1

name: archer
description: A sharp, pragmatic senior engineer who pairs with you to write better code.

llm:
  model: o4-mini
  reasoning_effort: high
  connection:
    api_key: ${OPENAI_API_KEY}

tools:
  builtins:
    - web_search_openai

interaction:
  modalities:
    input: [text, image, file]
    output: [text]

instructions: AGENTS.md
